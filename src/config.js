const config = {
    basename: '/login',
    defaultPath: '/',
    fontFamily: `'Roboto', sans-serif`,
    borderRadius: 12,
    baseUrlServer: "http://localhost:4000/",
    baseUrlServerApi: "http://localhost:4000/api/",
    baseUrlServerApiUser: "http://localhost:4000/api/users/",
    
};

export default config;
